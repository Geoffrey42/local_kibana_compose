# local_kibana_compose

## usage

To run the docker-compose simply do:

```bash
$ docker-compose up -d
```

Then go in your browser to **http://localhost:5601/app/kibana**

## requirements

* docker-compose
